# 更新记录

## 2026-08-08

- 修正仪表盘年份和月份栏的吸顶定位层级；向下浏览统计图表和清单时，该行会持续固定在页面顶部。
- 流水列表的全局搜索现在只要输入有效关键词就始终查询整个数据库，不再受当前月份或字段筛选状态限制；切换月份会清空搜索词并明确返回单月视图。
- 修复整表保存包含新增或复制流水时读取空基线行并触发 `KeyError: 时间` 的问题；基线差异比较现在仅处理已有流水，新记录继续由独立新增分支保存。

- 流水列表的“手动记账”改为本地表格组件内弹窗：打开、字段联动、输入校验和取消均不再触发页面刷新；仅在成功保存新流水后刷新一次当前流水区域。只读操作栏合并到流水表格组件，保持与编辑模式一致的本地交互。

- 修复流水列表点击“手动记账”后因本地表格动作未消费而持续刷新的问题；只读和整表编辑两种状态现在均可正常打开手动记账弹窗。

- 流水列表的只读和整表编辑工具栏均新增“手动记账”入口；点击后在与“修改流水”一致的大尺寸居中弹窗内录入单笔流水，编辑状态下会保留未保存草稿并合并当前已加载月份的新记录。
- 流水列表的只读和整表编辑工具栏均新增“手动记账”入口；点击后在与“修改流水”一致的大尺寸居中弹窗内录入单笔流水，编辑状态下会保留未保存草稿并合并当前已加载月份的新记录。

## 2026-08-08

- 重写流水修改和手动记账的日期/时间分段输入：点击年月日时分秒会直接选中对应数字，年份按四位输入，其余分段按两位输入，分段完成后自动跳转到下一段；单数字分段在切换、失焦或提交时自动补零，并保持 24 小时制格式。

## 2026-08-08

- 仪表盘顶部的年份和月份选择栏现为吸顶栏，向下浏览统计图表或报销清单时仍可直接切换月份。
- 仪表盘清单调整为仅显示待报销的公费垫付，并新增未转出的过手转入待转出清单；已结清和已转出的条目不再显示。
- 流水列表的只读表格和整表编辑表格统一固定为约 10 行可视高度，超出部分在表格内部滚动。
- 删除账户选项“手动录入”；数据库中没有流水使用该账户，后续启动也不会自动恢复。
- 手动记账新增 24 小时制时间输入，初始为 `00:00:00`，留空时也按当天 `00:00:00` 保存；整表编辑的时间字段填完当前日期或时间段后会自动选中下一段，并避免浏览器重复写入数字。
- 修正日期和时间分段输入的位置状态：输入 `14` 等两位字段时按第一位、第二位顺序写入，不再因旧光标位置产生 `11` 等重复数字。
- 点击流水月份按钮时会清空搜索词、退出全局搜索并切换为该月视图，避免出现搜索框有文字但仅查询当月的冲突状态；累计编辑草稿不受影响。
- 选项管理新增顺序列和上下箭头，支持账户、支出/收入分类及支出/收入标签调整展示顺序；新增、重命名、自动关联标签和删除均先保存在浏览器草稿，点击保存后统一写入数据库。
- 选项管理切换类型或离开页面时新增居中未保存修改弹窗，支持继续编辑、放弃并切换、保存后切换；保存成功使用页面内非弹窗提示。
- 收入分类的数据库展示顺序已调整为：工资收入、奖金收入、兼职收入、银行利息、转账收入、其它收入、过手转入、垫付报销。
- 删除整表修改表格外的累计草稿/月视图提示，并在表格左上角显示当前可见记录条数；条数会随筛选、排序和编辑视图即时更新。
- 流水表格的时间列和金额列表头下拉菜单新增升序排列、降序排列和取消排序；排序在本地表格内即时执行，不触发页面刷新，也不改变数据库和编辑草稿。

## 2026-08-07

- 流水表格及金额范围筛选中的金额输入框已隐藏浏览器自带上下调节箭头；日期编辑单元格移除日历按钮，仅保留固定格式的 24 小时文本输入。
- 流水表头筛选改为完全在本地表格内即时执行，不再向页面回传筛选事件或触发 Streamlit 重跑；切换月份或搜索条件时自动清空本地字段筛选。流水表格在记录较少时也保持固定高度，超出高度后继续使用表格内部滚动。
- 删除整表修改界面中关于跨月份累计草稿和本地草稿保存规则的两段提示文字，使操作区更紧凑。
- 修复流水片段局部重跑时因跳过页面初始化而缺少年份会话键导致的 KeyError；流水片段现在自带月份、年份和筛选状态兜底初始化。
- 修复表头筛选首次点击只刷新但未应用的问题：筛选条件更新后立即进行一次流水片段局部重绘，首次操作即可生效；无变化条件不会重绘，筛选路径不调用整页刷新。
- 修复报销状态修改后行底色未即时更新的问题；状态下拉变更现在会立即重绘当前表格，使过手转出和垫付报销的淡绿色状态即时生效。
- 流水表格行颜色补充覆盖“过手转出（已转出）”和“垫付报销（已结清）”，两类记录统一显示淡绿色。
- 金额编辑输入改为允许任意小数位，输入后自动按四舍五入保留两位小数；整表编辑草稿、单条修改和数据库写入均统一保存两位小数，舍入后为零的金额会被拒绝。
- 修改流水界面的取消修改、删除选中行、批量修改和恢复默认列布局等需要确认的操作统一使用居中弹窗，不再在表格下方显示独立横条；确认内容仅在弹窗内处理，页面高度随表格内容收束。
- 流水筛选、复制、删除、进入整表修改、保存和取消改为仅刷新流水区域；无变化的筛选操作不再回传，避免整页重绘和重复刷新。
- 整表修改支持在月份视图与全局搜索视图间切换：搜索显示所有月份的匹配草稿，点击月份按钮清空搜索词、退出全局搜索并回到当前月；两种视图共用累计浏览器草稿并统一保存或取消。
- 整表修改模式支持切换年份和月份；访问过的月份共用浏览器草稿，跨月修改、复制和删除可一次保存，取消时统一放弃，表头筛选继续只影响当前月份的可见行。
- 修复表头筛选重复回传导致的持续刷新；清除未启用筛选、清除全部或重复应用相同条件时不再进入刷新循环。账户、收支、分类、标签和报销状态改为勾选或取消勾选后立即筛选；金额范围继续通过“应用筛选”确认。
- 表头筛选入口改为下拉箭头图标；筛选菜单与对应列表头左侧对齐，并显示在该表头正下方。
- 修正整表修改界面的组件高度计算，页面在表格末尾自然结束，不再在表格下方保留多余空白；表格内部滚动保持不变。

## 2026-08-06

- 流水筛选已移入本地表格表头筛选图标：账户、收支、分类、标签、报销状态支持多选和“空白”选项，金额支持最低/最高范围；只读和整表编辑模式均可使用，编辑筛选不会丢失未显示草稿。

## 2026-08-06

- 整表编辑新增“批量修改”弹窗，统一支持设置收支、标签、分类和报销状态；四项设置按目标收支与分类动态提供有效选项，修改先保存在浏览器草稿，点击保存修改后才写入数据库。

## 2026-08-06

- 收入分类新增“奖金收入”，移除“生活费收入”后续选项；历史流水中的原有分类值继续保留，奖金收入不设置默认标签，可手动选择。

## 2026-08-06

- 流水列表的复制和删除遵循统一提交规则：只读模式操作后立即写入数据库；整表编辑模式只修改浏览器草稿，复制或删除在点击“保存修改”后才持久化，取消或放弃修改不会改动数据库。

## 2026-08-05

- 恢复流水日期列的日历选择入口，日期选择只替换年月日并保留原有时分秒；日期时间输入固定为 24 小时制，点击年、月、日、时、分、秒任一段会自动选中对应数字，便于直接修改。
## 2026-08-05

- 修复本地流水表格连续编辑时的焦点冲突，单元格修改后点击下一个单元格无需重复点击，蓝色激活框始终跟随当前单元格。

## 2026-08-05

- 锁定公费垫付、过手转出、过手转入和垫付报销的标签为固定空值，手动录入、单条修改、整表编辑及分类自动标签关联均禁止修改。

## 2026-08-05

- 所有交易日期时间的输入与展示统一为 24 小时制 `YYYY-MM-DD HH:MM:SS`，单条修改和整表修改不再受系统 12 小时制或 AM/PM 显示影响。

## 2026-08-04

- 新增“选项管理”页面，支持在应用内新增、重命名和删除账户、支出/收入分类及标签；重命名会同步已有流水，删除仅移出后续选项并保留历史记录；收入分类的自动标签关联也可维护。
- 收入新增劳动收入、财产收入和转移收入标签；工资收入、兼职收入、银行利息、生活费收入及其它收入会按分类自动关联对应标签，并同步支持手动记账、单条修改、整表修改和已有流水更新。
- 收支新增“退款”类型；退款沿用支出分类和标签，并在月度、年度分类、账户分布及结余统计中从支出金额扣除。
- 收入分类切换时会自动填入对应的建议标签，标签随后可按实际情况手动修改，不再被自动映射强制覆盖。
- 所有既有及新增流水金额统一按两位小数四舍五入并写入数据库，表格展示同步固定为两位小数。
- 仪表盘移除支出账户扇形图，新增收入标签占比和支出标签占比扇形图；支出标签统计会按标签扣除退款金额。
- 选项管理的支出分类（含退款）新增“自动关联标签”列，可维护支出分类到消费标签的建议关系；切换支出或退款分类时自动带入建议标签，之后仍可手动修改。
- 报销清单、账单导入预览、自动过滤记录和选项管理表格统一使用本地表格组件，支持固定表头、横向滚动、列宽调整和列顺序拖拽；选项删除保存前增加二次确认。

## 2026-08-03

- 修复 EXE 运行时缺少 `status_rules` 模块的问题，并在打包完成后自动检查业务模块与 Streamlit 首页资源是否齐全，避免生成启动后才报错的不完整程序。
- 重构 EXE 打包流程：固定使用 Python 3.14 和独立 `.venv-build` 环境，按完整锁定依赖构建；先在暂存目录执行真实 Streamlit 会话验收，再校验并保留原账本后替换正式版本，构建失败不再提前删除可用程序或数据。
- 窗口版启动失败时改为弹窗提示并写入 `startup-error.log`；打包脚本兼容发布目录被 Windows 占用时的原位同步更新。

## 2026-08-02

- 打包脚本现通过 Windows Python Launcher 使用已注册的 Python 3 环境，自动创建桌面“个人记账”快捷方式，并在依赖安装或打包失败时立即停止，避免生成无效入口；日常可双击 EXE 启动并自动打开浏览器，账本仍保存在程序同级 `data` 目录，原启动 BAT 保留兼容。

## 2026-07-13

- 扩展分类专属状态：垫付报销默认已结清、过手转入默认待转出且按待转出/已转出显示淡红/淡绿、过手转出默认已转出；公费垫付状态规则和仪表盘统计保持不变。

## 2026-07-13

- 流水全局搜索新增多关键词语法，支持 `AND` / `且` 与 `OR` / `或`，并按 AND 优先于 OR 组合匹配备注、分类和交易对方。

## 2026-07-13

- 整表修改中恢复单元格原生局部复制：在文本输入框内选中部分字符后可仅复制所选内容，未选中文字时仍复制当前单元格或区域。

## 2026-07-13

- 整表修改中恢复单元格原生局部粘贴：在输入框内粘贴普通文本时仅替换当前选中的字符，多行或制表符分隔内容仍支持跨单元格批量粘贴。

## 2026-07-13

- 流水字段筛选新增“空白”选项，可筛出未填写账户、收支、分类或标签的记录；金额筛选改为无加减步进按钮的数字文本输入。

## 2026-07-13

- 流水列表新增标签字段筛选，支持只读和整表编辑草稿按消费标签组合筛选。

## 2026-07-13

- 修复整表编辑期间筛选后内容复原的问题：编辑草稿按会话独立保存，筛选始终基于完整草稿，单元格、粘贴、批量分类、批量标签及草稿删除会保留到保存或放弃修改。

## 2026-07-13

- 修复编辑期间使用筛选后草稿复原的问题：筛选重跑会恢复全部未保存单元格修改，直到保存修改或放弃修改。

## 2026-07-13

- 编辑流水期间开放账户、收支、分类和金额字段筛选；筛选只改变可见草稿行，隐藏行的未保存修改仍会在保存时一并提交。

## 2026-07-13

- 整表修改模式新增“删除选中行”：二次确认后仅从浏览器草稿移除，点击保存修改时才批量删除数据库流水；取消或放弃修改不会删除记录。

## 2026-07-12

- 整表修改新增“设置分类”批量操作：仅在勾选同一种收支流水时启用，并只显示对应收支分类；可在浏览器草稿中批量设置或清空分类，保存修改后才写入数据库。

## 2026-07-12

- README 更新为完整使用说明，覆盖仪表盘、流水列表、本地表格、账户分类标签、公费报销、过手往来、各类导入、备份、启动和打包方式。

## 2026-07-12

- “过手转出”和“过手转入”纳入非个人收支口径，已从收支卡片、趋势图、分类与账户统计、年度支出汇总中排除；仪表盘新增当月过手转出与转入金额展示。

## 2026-07-12

- 新增“过手转出”支出分类和“过手转入”收入分类，已同步到手动记账、单条修改和整表编辑的分类选项。

## 2026-07-12

- 单条修改弹窗调整为大尺寸三列四行布局：备注独占整行，减少纵向空间占用并保留完整编辑空间。

## 2026-07-12

- 调整流水列表修改操作样式：单条“修改选中行”使用主按钮样式，整表“修改流水”使用普通按钮样式，并移除两处铅笔图标。

## 2026-07-12

- 历史 Excel 导入兼容“支出表”及 Sheet3 的“备注 发红包的人”列，并保留支出流水标签。
- 已导入 `记账2.xlsx`：新增支出表 184 条、Sheet1 19 条、收入表 23 条、Sheet3 30 条，共 256 条流水。

## 2026-07-12

- 恢复流水列表的单条修改入口：只读列表勾选一条流水后可直接打开预填修改窗口，保存后刷新列表；多选或未选时按钮保持禁用。

## 2026-07-12

- 流水列表搜索改为全局搜索：输入关键词时查询整个数据库，清空关键词后恢复当前月份显示。

## 2026-07-12

- 支出标签新增“人情往来”，同步支持手动记账、单条修改、整表编辑和批量设置。

## 2026-07-12

- 年度分类支出汇总表的“支出类型”列改为加粗显示。

## 2026-07-12

- 年度分类支出汇总表移除列宽调整功能及“恢复默认列布局”按钮，流水列表相关功能保持不变。

## 2026-07-12

- 年度分类支出汇总表内字体调整为 12px，其他页面字体保持不变。

## 2026-07-12

- 侧边栏固定为 240px，并隐藏浏览器中的拖拽调整手柄，禁止随意改变宽度。

## 2026-07-12

- 缩小年度分类支出汇总的列宽与字号，固定 12 个月内容在页面内显示并隐藏横向滚动条。

## 2026-07-12

- 调整仪表盘内容顺序，将年度分类支出汇总移动到月度收支趋势上方。

## 2026-07-12

- 修正仪表盘年份控件与月份按钮的垂直对齐，并确保常态下显示当前选定年份。

## 2026-07-12

- 仪表盘年份下拉与 1—12 月按钮调整为同一行显示，按钮间距统一为 10px。

## 2026-07-12

- 修复流水列表全选删除：删除时读取最新勾选状态，并以单次 SQLite 事务批量删除，避免漏删和界面卡顿。

## 2026-07-12

- 重新读取 `记账2023-2026.xlsx`，将 Sheet1、Sheet2 中的生存刚需、品质生活、自我投资标签同步到已有流水；空白标签保持为空，并按 import_hash 去重匹配。

记录个人记账系统每次更新后新增或改善的成果。后续每次完成版本迭代后，请在本文档顶部追加一条记录。

## 2026-07-12

### 仪表盘年度分类支出汇总

- 新增按当前仪表盘年份展示的 1—12 月支出分类热力汇总表，复用本地表格组件并按金额深浅着色，缺失月份补零。
- 年度表分类来源于支出分类列表，并遵循个人统计口径排除公费垫付和垫付报销。

### 消费标签与批量设置

- 支出流水新增生存刚需、品质生活、自我投资三种可选标签；收入标签自动保持为空。
- 编辑模式支持勾选行、表头全选和本地批量设置标签，确认后仅写入草稿，点击保存修改后才持久化。

### 账户与备注字段统一

- 流水列表、编辑器、筛选、搜索和表单统一使用“账户”与“备注”，内部字段同步改为 `account` 与 `remark`。
- SQLite 启动时自动将旧 `platform`、`description` 列迁移并保留现有流水与导入去重信息。

### 历史 Excel 账目导入

- 支持导入记账 2023—2025 Excel 的 Sheet1、收入表、Sheet2 和 Sheet3 流水，日期缺少时分秒时自动按当天零点保存。
- 保留历史账户与分类原值，缺失的对方、支付方式、标签和报销状态均保持空白，并使用现有去重规则防止重复导入。
- 已从历史 Excel 新增 2710 条流水；文件内及数据库中已有的 10 条重复记录均已自动跳过。

## 2026-07-11

### 流水列表本地单击编辑

- 流水整表修改改用随项目分发的本地表格组件，单击单元格即可输入或打开下拉选项。
- 编辑草稿保留在浏览器组件内，编辑过程中不刷新页面、不写入数据库；仅点击保存后才回传并提交整表修改。
- 只读流水表同步改用本地组件，行复选框和表头全选在浏览器端即时完成，不再触发 Streamlit 页面刷新；删除选中行时才提交选择结果。
- 本地表格恢复列宽调整、列顺序拖拽、键盘导航与复制粘贴操作，并在当前浏览器记忆列布局；可一键恢复默认布局。
- 只读流水的恢复布局、修改与删除按钮统一调整到字段筛选栏下方，避免占用表格右上区域。
- 恢复默认列布局加入二次确认；日期列支持仅日期与精确到秒的显示切换，点击表格单元格后保持当前滚动位置。

### 流水列表无刷新编辑

- 流水列表默认改为只读表格；点击“修改流水”后才进入当前可见记录的整表编辑模式。
- 整表编辑在表单中暂存，连续修改单元格不会写入数据库或刷新页面；仅点击“保存修改”后统一校验并保存。
- 取消修改增加放弃确认，可继续编辑或丢弃整表草稿；编辑期间锁定导航、月份、搜索、筛选、选择和删除。
- 表格复选框选择与全选改为流水列表局部刷新，避免勾选操作重载整页内容。

### 主页面顶部显示修复

- 调整全局主内容顶部留白，仪表盘、流水列表、导入账单和手动记账的首行控件均可完整显示，不再被顶部工具栏遮挡；留白采用 3rem 以适配顶部工具栏。
- 增大流水列表筛选切换按钮高度；展开字段筛选时，按钮与筛选控件保持同一行展示。
- 流水列表的年份、12 个月按钮和关键词搜索改为同一行紧凑布局。
- 流水列表年份框与月份按钮调整至约 75px 和 30px，每个年份/月度选项之间保留 5px 空白，搜索框同步扩展。
- 流水列表字段筛选栏改为顶部常驻显示；右侧“取消筛选”仅清空筛选条件。
- 新增公费垫付与报销管理：支出可选“公费垫付”、收入可选“垫付报销”；公费流水支持待报销/已结清状态、淡红/淡绿行底色、报销 KPI 和跟踪清单，个人统计自动排除公费相关流水。

## 2026-07-11

### 流水列表交互与筛选优化

- 流水列表改为按所选月份一次性显示全部记录，移除了分页和页码输入。
- 月份选择改为“年份下拉框 + 12 个月按钮”：有流水的月份可选，空月份置灰；切换年份时自动定位到该年最新有流水的月份。
- 仪表盘同步采用“年份下拉框 + 12 个月按钮”布局，并与流水列表使用独立的选择状态。
- 流水列表新增表格内编辑、单条弹窗修改、批量保存、撤销未保存修改与删除选中记录功能。
- 增加未保存修改保护：切换页面、年份、月份、关键词或字段筛选时，可继续编辑、放弃后切换或保存后切换。
- 选择列增加“全选 / 取消全选”单按钮切换，不影响尚未保存的业务字段编辑。
- 增加展开式字段筛选：支持来源、收支、分类及金额区间；筛选条件变更后自动应用，并与关键词搜索叠加。
- 字段筛选采用“筛选 / 取消筛选”两态按钮；取消筛选会清空条件并关闭筛选栏。

### 页面布局优化

- 移除仪表盘、流水列表、导入账单和手动记账页面的大标题，内容区更紧凑。
- 收紧主内容区域顶部留白，同时保留侧边栏导航和图表内容标题。
- 流水列表重排为顶部控件区与下方表格区：年份、月份、搜索和筛选集中展示，表格使用固定高度滚动区域。
- 流水列表的全选、修改、保存、撤销和删除操作按钮移至顶部控件区；月份按钮改为两行稳定排列，避免宽屏布局错位。

## 2026-07-10

### 导入过滤与 XLSX 兼容性

- 支付宝账单导入自动过滤“余额宝收益发放”“花呗自动还款”“银行卡定时转入”，过滤记录不写入数据库。
- 微信 CSV/XLSX 导入自动过滤“转入零钱通”；同时检查商品/说明与交易类型映射后的分类字段，兼容微信 XLSX 的列结构。
- 微信 XLSX 导入可正确完成：共 332 条记录中保留 309 条，自动过滤 23 条。
- 修复上传 XLSX 文件的后缀识别：读取至 `BytesIO` 后保留原始文件名，避免误按 CSV/UTF-8 读取并报编码错误。

### 自动过滤记录复核

- 自动过滤记录会追加到当前浏览器会话的历史过滤记录中，并在导入页提供当次平台记录和统一历史记录查看。
- 历史过滤记录包含来源平台字段，可区分支付宝与微信；仅点击“清除过滤记录”才会清空。
- 该记录功能为 Streamlit 会话级，不写入 SQLite；服务重启、会话重建或关闭浏览器后会清空。
